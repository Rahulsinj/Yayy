import { portfolioData as data } from '@/data/portfolioData';import { Section } from '@/components/Section';
export default function About(){return <><Section eyebrow="Profile" title={`${data.profile.name} — ${data.profile.role}`}><div className="grid gap-4 md:grid-cols-2"><div className="border border-line bg-panel p-8"><p className="text-lg leading-8 text-muted">{data.profile.intro}</p><p className="mt-8 font-mono text-xs uppercase tracking-[.3em] text-muted">{data.profile.location}</p></div><div className="grid gap-3">{data.languages.map(l=><div className="border border-line bg-panel p-5" key={l.name}><span className="text-4xl">{l.name}</span><p className="mt-2 text-sm text-muted">{l.note}</p></div>)}</div></div></Section><Section eyebrow="Timeline" title="Institutional learning, applied builds, and maintainable habits."><div className="grid gap-4">{data.timeline.map(item=><div className="grid gap-4 border border-line bg-panel p-6 md:grid-cols-4" key={item.title}><p className="font-mono text-xs uppercase tracking-widest text-muted">{item.period}</p><h3 className="text-2xl md:col-span-1">{item.title}</h3><p className="text-muted md:col-span-2">{item.description}</p></div>)}</div></Section></>}
