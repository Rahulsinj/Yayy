'use client';import { useMemo,useState } from 'react';import { portfolioData as data } from '@/data/portfolioData';import { ProjectCard } from '@/components/ProjectCard';
export default function Projects(){const tags=['All',...new Set(data.projects.flatMap(p=>p.tags))];const[active,setActive]=useState('All');const projects=useMemo(()=>active==='All'?data.projects:data.projects.filter(p=>p.tags.includes(active)),[active]);return <section className="mx-auto max-w-7xl px-4 py-16"><p className="font-mono text-xs uppercase tracking-[.35em] text-muted">Archive</p><h1 className="mt-4 text-5xl tracking-[-.06em] md:text-7xl">Project index</h1><p className="mt-6 max-w-2xl text-muted">Filter Deeptanshu's work by tag. New project cards appear automatically when added to the centralized data file.</p><div className="my-10 flex flex-wrap gap-2">{tags.map(t=><button onClick={()=>setActive(t)} className={`border px-4 py-2 font-mono text-xs uppercase tracking-widest ${active===t?'border-white bg-white text-black':'border-line text-muted'}`} key={t}>{t}</button>)}</div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{projects.map(p=><ProjectCard key={p.title} project={p}/>)}</div></section>}
