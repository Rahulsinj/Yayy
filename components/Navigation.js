'use client';
import Link from 'next/link';import { usePathname } from 'next/navigation';import { portfolioData } from '@/data/portfolioData';
export function Navigation(){const path=usePathname();return <header className="sticky top-0 z-40 border-b border-line bg-black/75 backdrop-blur-xl"><nav className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><Link href="/" className="font-mono text-sm uppercase tracking-[.35em]">Deeptanshu</Link><div className="flex gap-1 rounded-full border border-line bg-panel p-1">{portfolioData.nav.map(n=><Link key={n.href} href={n.href} className={`rounded-full px-3 py-2 text-xs uppercase tracking-widest transition ${path===n.href?'bg-white text-black':'text-muted hover:text-white'}`}>{n.label}</Link>)}</div></nav></header>}
